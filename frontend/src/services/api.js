// Este arquivo é um bom lugar para centralizar as URLs da API
const API_URL = 'http://localhost:3001/api';

// No futuro, podemos colocar as funções de fetch aqui
// ex: export const fetchVagas = () => fetch(`${API_URL}/vagas`);